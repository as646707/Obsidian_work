some things to remember -
to import and export a component and what are default functions and what are named functions ?
default functions - they hold the main function of a file like in cpp and they are the ones that are the default function exported when we do something like import < file name  > from 'file.js '
**properties of the default functions -**

# jsx#

**what is jsx ?** 
 just using of java scirpt varibles and functions and object into mark up 
 
 **how to use it ?**
 1. {} for using variables .
 2. {{ }} for using objects and functions .
 **questions that needs to be dealt with** 

3. *is the function a javasript or a markup meaning the functions ( like default and named ) are they a javascript function or something else .*
**ans -** they are java script function that use the react libraries and change it to a react javascript fnctions and they are used to render ui in a react framework with the help of jsx ( special markup that is a js objects that renders ui ).

4. *so a jsx is a method of importing the js stuff back in the markup part ? but isnt the markup part inside a js ?*
 **ans** - no the jsx is just tag that renders ui that is all its also have a special property which allows us to use the java script objects functions and variables inside the markup with the help of the curly braces {}
5. *are the varible scopes till the functions in which they are initialised ?*
**ans** - not really if that were the case how could you use the the {} to call it inside the markup ? 
  example to illustrate the 
6. *if you want to use a varible why do you need to use the {} at all i mean arent they global variable and can be called from any where ?*
ans -
for further reading and more interactive questions kindly please go [[jsx and components problems ]]